#include <bits/stdc++.h>
using namespace std;

#define PRINTVAR(x) (cout << #x << " is " << x << endl)

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int a, b, c, d;
        cin >> a >> b >> c >> d;

        int x, y, z;
        x = b;    
        y = c;
        z = c;

        cout << x << ' ' << y << ' ' << z << '\n';
    }

    return 0;
}

